# UI Components

This directory is the local shadcn/ui catalog configured by `components.json`.

Keep components here when they are used by the app, generated by shadcn, or locally customized as part of the app's design system. Do not treat this directory as an unreviewed vendor dump: files should pass Biome, and unused generated components should be pruned only after checking `components.json` and local customizations.
